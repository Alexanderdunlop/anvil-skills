# Unrouted

- `fb-0003` seeded a process file from an assumption rather than from anything
  the user said. The lesson is about `/setup`, and §5 has no `process:setup`
  key. (2026-08-07)
- `fb-0007` hand-written entries have no capturing command. `manual` used for
  the `command` field; §4.6 enumerates no values. (2026-08-10)
- `fb-0008` behavioural thresholds count distinct tickets, and every entry
  captured before `/scope` exists has `ticket: null`. (2026-08-10)
